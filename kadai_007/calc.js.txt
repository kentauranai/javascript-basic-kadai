let num = 15;

// ① 3と5の両方の倍数を先に判定（最重要）
if (num % 3 === 0 && num % 5 === 0) {
  console.log("3と5の倍数です");

// ② 3の倍数
} else if (num % 3 === 0) {
  console.log("3の倍数です");

// ③ 5の倍数
} else if (num % 5 === 0) {
  console.log("5の倍数です");

// ④ それ以外
} else {
  console.log(num);
}